# frontend

Vue 3 + Vite。第 1 周由梁先楷执行 `npm create vite@latest . -- --template vue`（若目录非空则在临时目录创建再移入）。

约定：

- 开发代理把 `/api` 转到 `http://127.0.0.1:8000`
- PC 大屏默认路由 `/`，移动端同一路由响应式
- 地图 Leaflet，图表 ECharts，首页星球 Three.js 可选
- mock 字段必须与接口文档相同
