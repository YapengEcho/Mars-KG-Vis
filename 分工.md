# 成员分工（RACI）

项目组 4 人，符合「5 人以内、组长 1 名」。下面是执行版分工：谁做、做到哪、不做什么。交老师的一页纸用 [选题与分工-提交稿.md](选题与分工-提交稿.md)。

## 总原则

- 先保课程四件套：**爬取/采集、清洗挖掘、数据库、可视化预测与推荐**。
- 图谱、RAG、语音、手势是加分，排在四件套之后。
- 每个人有一个「演示时必须能讲出来」的模块，避免报告里写了现场没有。

## 崔亚鹏（组长 / 后端）

**做：** GitHub 仓库与分支权限、每周例会与看板、PR 审核合入；Django + DRF 骨架；MySQL 表结构与滕佳杰共同冻结；REST 接口；CORS；Redis 缓存（列表接口、统计、RAG 答案）；Docker Compose 中的 MySQL / Redis / Neo4j。

**不做：** 不写前端页面，不训练模型，不在 `main` 上直接改业务代码。

**演示负责：** 系统能跑、接口稳定、Git 提交记录干净。

**技术：** Python、Django、DRF、MySQL、Redis、Docker、Git。

## 滕佳杰（数据 / 图谱）

**做：** 锁定数据源并完成至少一条爬虫；pandas 清洗与特征（坐标、类型、尺度、坡度等）；定义实体关系；Neo4j 入库与 Cypher；同步写入 MySQL；提供图谱子图查询所需的数据。

**不做：** 不调大模型，不负责 Vue。MySQL 表结构变更必须和组长一起改，禁止两套 schema。

**演示负责：** 数据从哪来、清洗了什么、图谱上能展开一类滑坡的关系。

**技术：** Python、requests、pandas、Neo4j、MySQL。

## 梁先楷（前端 / 多模态）

**做：** Vue3 可视化大屏（分布地图、分类统计、图谱网络、预测/推荐结果、智能体对话框）；PC 优先、移动端响应式；语音指令（放大、切换、提问等，先做 5 条）；手势控制作为冲刺项（缩放/旋转，仅 PC）。

**不做：** 不单独开发 iOS/Android；Three.js 只做首页火星球，分析页用 Leaflet + ECharts；手势做不完时保语音。

**演示负责：** 2 分钟视频的画面。大屏要好看、能联动。

**技术：** Vue3、Vite、Element Plus、ECharts、Leaflet、Three.js、Web Speech API、MediaPipe。

## 周璇（算法 / 测试 / 交付）

**做：** 滑坡类型预测（sklearn）；相似滑坡推荐；LangChain + Chroma + 通义千问的 RAG（可检索清洗文本与图谱摘要）；冒烟测试与联调清单；报告里 AI / 优化 / AI 协助开发章节；2 分钟视频脚本并组织录制。

**不做：** 不上 Milvus，不上深度学习作为主模型；Redis 归后端；不要把测试理解成写几百条用例，以「演示路径不崩」为准。

**演示负责：** 问一句人话能答、预测/推荐有结果、报告和视频能交。

**技术：** Python、scikit-learn、LangChain、DashScope（通义千问）、Chroma、pytest。

## 交叉接口（必须对上）

| 产出 | 负责人 | 接收人 |
| --- | --- | --- |
| MySQL 表结构 | 崔亚鹏 + 滕佳杰 | 全组 |
| `/api/landslides*` `/api/stats*` | 崔亚鹏 | 梁先楷 |
| `/api/graph/subgraph` | 崔亚鹏（查库）+ 滕佳杰（数据） | 梁先楷、周璇 |
| `/api/predict` `/api/recommend` `/api/agent/ask` | 周璇算法 + 崔亚鹏挂接口 | 梁先楷 |
| 清洗后的文本与图谱摘要 | 滕佳杰 | 周璇做向量库 |
| 大屏交互事件（语音词表） | 梁先楷 | 全组按词表接指令 |

接口字段见 [docs/03-里程碑与接口约定.md](docs/03-里程碑与接口约定.md)。

## 原方案里需要改掉的点

1. Redis 不要同时写在组长和周璇名下，归后端。
2. 周璇原分工没有「预测/推荐」，但课程基本项要求有，现已补上。
3. Flask 与 Django 二选一：选 Django，与老师示例和加分项一致。
4. Milvus、Scrapy、原生移动端、JWT 登录从主路径拿掉，需要时再加。
